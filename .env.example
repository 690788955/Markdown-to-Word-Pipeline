# ==========================================
# 运维文档生成系统 - 环境变量配置示例
# ==========================================
#
# 复制此文件为 .env 并根据需要修改配置
# cp .env.example .env

# ==========================================
# 端口配置
# ==========================================
# 服务端口 (默认: 8080)
# 有效范围: 1024-65535
# 示例: PORT=9000
PORT=8080

# ==========================================
# 管理密码配置
# ==========================================
# 管理密码 - 用于锁定/解锁客户配置
# 默认: admin123
# 建议在生产环境中修改为强密码
ADMIN_PASSWORD=admin123

# ==========================================
# 目录配置 - 方式1: 单一根目录
# ==========================================
# 文档根目录 - 包含 src/, clients/, templates/ 子目录
# 使用此选项时，系统会自动查找子目录
# 示例: DOCS_DIR=/home/user/documents
# DOCS_DIR=.

# ==========================================
# 目录配置 - 方式2: 分别指定各目录
# ==========================================
# 源文档模块目录 (Markdown 文件)
# 默认: ${DOCS_DIR}/src 或 ./src
# SRC_DIR=/path/to/source/modules

# 客户配置目录 (客户特定的配置文件)
# 默认: ${DOCS_DIR}/clients 或 ./clients
# CLIENTS_DIR=/path/to/client/configs

# Word 模板目录 (docx 模板文件)
# 默认: ${DOCS_DIR}/templates 或 ./templates
# TEMPLATES_DIR=/path/to/word/templates

# 自定义字体目录 (ttf/otf 字体文件)
# 默认: ${DOCS_DIR}/fonts 或 ./fonts
# 将字体文件放入此目录，容器启动时会自动加载
# FONTS_DIR=/path/to/custom/fonts

# 输出目录 (生成的文档保存位置)
# 默认: ./build
# OUTPUT_DIR=/path/to/output

# ==========================================
# Docker 镜像配置
# ==========================================
# 自定义 Docker 镜像 (可选)
# 默认: doc-generator:latest
# IMAGE=your-registry/doc-generator:v1.0

# ==========================================
# 配置示例场景
# ==========================================

# 场景1: 开发环境 - 使用默认配置
# PORT=8080
# (其他变量使用默认值)

# 场景2: 生产环境 - 自定义端口和输出目录
# PORT=9000
# OUTPUT_DIR=/var/lib/doc-generator/output

# 场景3: 多项目环境 - 完全自定义路径
# PORT=9001
# DOCS_DIR=/projects/project-a/docs
# OUTPUT_DIR=/shared/output/project-a

# 场景4: CI/CD 环境 - 分离的目录结构
# PORT=8080
# SRC_DIR=/workspace/docs/modules
# CLIENTS_DIR=/workspace/configs/clients
# TEMPLATES_DIR=/workspace/templates
# OUTPUT_DIR=/workspace/artifacts
# IMAGE=registry.company.com/doc-generator:latest

# 场景5: 本地测试 - 避免端口冲突
# PORT=8081
# OUTPUT_DIR=./test-output

# ==========================================
# 注意事项
# ==========================================
# 1. 端口范围必须在 1024-65535 之间
# 2. 目录路径可以是相对路径或绝对路径
# 3. 个别目录变量 (SRC_DIR, CLIENTS_DIR 等) 优先于 DOCS_DIR
# 4. 确保指定的目录存在且有适当的读写权限
# 5. 输出目录会自动创建（如果不存在）
# 6. 使用 docker compose config 验证配置是否正确