# 运维手册 配置
# 自定义生成

client_name: 长沙银行
template: default.docx
modules:
    - src/13-变量模板示例.md
pandoc_args:
    - --toc
    - --number-sections
    - --standalone
output_pattern: '{client}_运维手册_{date}.docx'
